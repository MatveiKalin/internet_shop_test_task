5 июля 2019:
Исправить физическую модель своего испытателььного задания для СУБД "Oracle".

1). "number всегда просто number без ограничений размерности".

+ В типе "number" на диаграме убрать размерности.


2). "название таблицы не должно превышать 25 символов". 

+ Все названия таблиц должны быть не больше 25 знаков.


3). "первичный ключ всегда в формате <название таблицы>_id".

+ Исправить названия первичных ключей с id на <название таблицы>_id.


4). "первичный ключ всегда заполняется из секвенции и не будем быть FK".

+ Не должны быть составные первичные ключи. Всегда должен быть суррогатный первичный ключ.


5). "название - <название таблицы>_name".

+ Поле "name" или "title" должно называться <название таблицы>_name.


6). + Дата всегда date, только в исключительных ситуациях где нужны милисекунды там timestamp


7). + Первичный ключ должен быть вверху.


8). + Следующие таблицы удалить:
discount
rack
method_delivery
user_type
gender
favorite
user_comment


9). +"Добавь версионность стоимости товара, она может менятся во времени".


10). +История изменения статуса заказа.


6 июля 2019:
11). +Удалить таблицу "method_delivery".

12). +Удалить поле "basket.goods_id".

13). +В таблице "user" удалить все поля, кроме "login" и "pass", другие поля не особо нужны.

14). +Исправить в физической модели стрелку (связь) между таблицами "history_status_order" и "order".

15). +Добавть в поле в таблицу "order" поле "status_order_id".

16). +Добавить в таблицу "order_detail" поле "amount". И сделать так, чтобы был вторичный ключ был свзяан с первичным ключом таблицы "goods_price".

17). +Справочник категорий товаров нужно сделать иерархическим, чтобы одна категория была потомком от одного родителя.

18). +Сделать товар, чтобы находился в нескольких категориях.

19). +Предложить, как можно сделать контроль корректности перехода по статусам. 

Я предложил в виде функций только написал на JS, в будущем можно перевести на язык PL/SQL.

20). +-На все внешние ключи нужно создать индексы.

21). +Даты активности строки изменить на 
active_from
active_to


8 июля 2019:
22). +Индексы сделать так:
i_<сокращенное имя таблицы>_<сокращенное имя индексируемых колонок>

констрейнт также является объектом БД и его именуем как правило как и индекс 
fk_<сокращенное имя таблицы>_<сокращенное имя индексируемых колонок>
Исправить наименования ограничений на внешние ключи в свойствах связи (Reference Proporties).
fk_<сокращенное имя таблицы>_<сокращенное имя индексируемых колонок>
23). +category_inherit_category это не нужно, достаточно добавить родительский ид в category_goods

24). +таблицы правил перехода по статусам не вижу.

25). + Зделать так, чтобы индексы индексировали.

26). +Изменить на такой формат:
i_<сокращенное имя таблицы>_<сокращенное имя индексируемых колонок>
fk_<сокращенное имя таблицы>_<сокращенное имя индексируемых колонок>

27). +Изменить названия CONSTRAIN по виду:
fk_<сокращенное имя таблицы>_<сокращенное имя индексируемых колонок>

28). +Проверить, чтобы навзаняи соответсвовали правилам.
в оракле зашито ограничение на названия 30 мисволов, но лучше останавливатся на 29

название индекса не более 28 символов, 
чтобы при преобразовании в название констрейнта получалось не более 29

названия колонок не более 27 символов, 
так как переменные под них потом обзываются с префиксом l_ - для локальных и p_ для параметров проц

назание таблицы 25 символов,
потому что потом создаются процы на изменение данных с префиксами del_ ins_ upd_



9 июля 2019
29). +Исправить наименования ограничений на внешние ключи в свойствах связи (Reference Proporties).
fk_<сокращенное имя таблицы>_<сокращенное имя индексируемых колонок>

30). +Поставить NOT NULL у тех полей, где это необходимо.

31). +Изменить названия таблиц и полей так, чтобы не было кавычек.

32). +У поля "goods_price.active_to" NOT NULL задать на физической модели и задать значение по умолчанию для этого поля: 01.01.4000. 

33). +добавить в таблицу товаров атрибуты
длинна
ширина
высота

34). +В таблицу "category_goods" добавить поле "order_by", чтобы можно было задать их сортировку, сортировка осуществляется в рамках родительской категории, т.е. это не сквозной номер.

36). Данных в БД должно быть столько сколько требуется для проверки корректности выполнения запросов, не больше.
То есть заполнить необходимые таблицы.

35). Создать пакет и в нем процедуры:
1 отдает иерархический список категорий начиная с тех в которых есть товары с заведенной стоимостью 
поля курсора: 
ид категории
ид родительской категории
название
флаг является ли узел листом

2 курсор который по ИД категории отдает перечень 
поля курсора
ид товара
название
текущая цена

3. курсор который по ИД товара одает данные
поля курсора
название
текущая цена
длинна
ширина
высота

4. Проца добавления товара в корзину